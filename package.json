{
  "name": "fae-v2",
  "private": true,
  "description": "FAE-v2 monorepo — voice agent with long-term memory",
  "scripts": {
    "setup": "cd backend && uv sync --group dev && cd ../ui && pnpm install",
    "dev": "concurrently -k -n backend,ui -c green,cyan \"npm run dev:backend\" \"npm run dev:ui\"",
    "dev:backend": "cd backend && uv sync --group dev --quiet && uv run uvicorn fae.api:app --reload --host 127.0.0.1 --port 8000",
    "dev:ui": "cd ui && pnpm dev",
    "test:backend": "cd backend && uv run pytest",
    "build:ui": "cd ui && pnpm build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=20"
  }
}
