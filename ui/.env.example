# Browser-visible API base (backend). Used at build time for Docker too.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional defaults for the Agent settings panel
NEXT_PUBLIC_LLM_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
NEXT_PUBLIC_LLM_MODEL=qwen3-max
